df = pd.read_csv(
    "data.csv", 
    sep=None,              # 自動偵測分隔符號（逗號、分號或 Tab）
    engine="python",       # 使用 Python 引擎（容錯率較高，能處理不規則資料）
    on_bad_lines="skip"    # 如果某一行格式真的錯誤，自動略過該行而不崩潰
)@st.cache_data
def load_local_data():
  import os

  if not os.path.exists("data.csv"):
    return pd.DataFrame(columns=["Date", "TAIEX", "00631L", "NDFI", "PCR_5MA"])

  try:
    raw_df = pd.read_csv("data.csv")
    # 將無法轉換的非日期格式（如 ... 或文字）自動轉為 NaT，避免程式崩潰
    if "Date" in raw_df.columns:
      raw_df["Date"] = pd.to_datetime(raw_df["Date"], errors="coerce")
      raw_df = raw_df.dropna(subset=["Date"])  # 過濾掉日期無效的行
    return raw_df
  except Exception as e:
    st.error(f"讀取 data.csv 發生錯誤: {e}")
    return pd.DataFrame()
Date,TAIEX,00631L,NDFI,PCR_5MA
2026-01-02,20000,150,1.2,1.05
2026-01-03,20100,152,1.3,1.08
